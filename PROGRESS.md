# Build Progress Log

> Source of truth for build state across sessions. At the start of any new session, read this
> file (after `CLAUDE.md`) to identify the last "complete" module and the next "not started"
> module, then cross-check against `git log --oneline -20` before resuming work.

| Module ID | Module Name | Status | Date completed | Test results summary | Known issues / follow-ups |
|---|---|---|---|---|---|
| M01 | Project Scaffold & Config | complete | 2026-06-30 | 64 tests passing (11 Redis-integration + 53 unit), 99% coverage on shared/+apps/, ruff clean, mypy --strict clean, docker-compose up: all 7 services Up/healthy | poetry.lock not yet generated -- Dockerfile.base installs a minimal pip subset, not full pyproject.toml (ADR-004); pandas-ta repinned 0.3.14b0->0.4.71b0 (original pin missing from PyPI); TA-Lib C source build fails against modern GCC, deferred to M04; env vars introduced: APP_ID, ENVIRONMENT, LOG_LEVEL, TRADING_MODE, LIVE_TRADING_CONFIRMED, REDIS_URL, POSTGRES_DSN, TIMESCALE_DSN |
| M02 | Market Calendar & Session Manager | complete | 2026-06-30 | 144 tests passing (1-2 skipped depending on network/Redis availability), 98% coverage on shared/session_manager.py, ruff clean, mypy --strict clean, docker-compose up: all 7 services Up/healthy, both per-app containers verified post-change | ASX holiday endpoint unconfirmed (404) -- Australia raises CalendarUnavailableError on weekdays until fixed (ADR-006), fails closed by design, not silent; NSE live fetch confirmed working; fixed a structlog cache_logger_on_first_use bug found via this module's own tests (logging.py); RegionConfig gained pre_market_local + snapshot_window_start_local fields; no new env vars |
| M03 | High-Throughput Buffering & Storage | complete | 2026-06-30 | 189 tests passing (2 skipped: ASX holiday endpoint, yfinance rate-limited), 99% coverage on shared/+apps/, 100% on shared/storage/, ruff clean, mypy --strict clean, docker-compose up: all 7 services Up/healthy, schema verified idempotent against both standalone and compose-managed TimescaleDB | ohlcv_1m stores finest-available granularity not strictly 1-min bars (ADR-007); continuous aggregates need materialized_only=false for real-time aggregation -- found via live testing, not assumed (ADR-008); yfinance/Yahoo Finance rate-limited (HTTP 429) from this sandbox for any ticker, same pattern as M02's NSE/ASX -- backfill CLI verified to fail gracefully (rows=0, no crash); SQLite failover buffer at shared/data/buffer.db (gitignored); new env var: none (TIMESCALE_DSN already existed from M01) |
| M04 | Core Technical Indicator Engine | complete | 2026-06-30 | 224 tests passing (2 skipped: ASX holiday endpoint, yfinance rate-limited), 99% coverage on shared/+apps/, 100% on shared/indicators/ except 2 genuinely-defensive unreachable branches, ruff clean, mypy --strict clean, docker-compose up: all 7 services Up/healthy, live VERIFY against TimescaleDB+Redis: 240x 5m candles, 16 indicators computed, cached, latency ~2-5ms (budget 50ms) | TA-Lib resolved via prebuilt wheel 0.6.8 (no C build needed) -- supersedes M01's deferred build issue (ADR-004); pandas-ta DROPPED, replaced by direct NumPy/pandas for the 4 non-TA-Lib indicators (VWAP, VWAP bands, Volume Delta, Pivot points) to avoid forcing a breaking numpy/pandas bump (ADR-009); Volume Delta is a close-vs-open proxy, not true tick-level aggressor volume (needs M16); VWAP session boundary uses UTC calendar date, exact for NSE/ASX today but documented as a simplification; registry pattern means adding an indicator = one new file in shared/indicators/definitions/ + one import line; new env var: none |
| M05 | Instrument Master & Corporate Actions | complete | 2026-06-30 | 299 tests total (297 passing + 2 skipped with Redis/TimescaleDB up; offline otherwise), 98% coverage on shared/+apps/, ruff clean, mypy --strict clean, docker-compose up: all 7 services Up/healthy, live VERIFY: real NSE fetch found PGIL's actual 10->5 split and RELIANCE's real dividend history; synthetic-split VERIFY confirms adjusted series ratio + M04 ATR computed on adjusted vs raw indicators differ as expected | NSE instrument master + corporate actions both live-confirmed (archives.nseindia.com CSV, nseindia.com corporate-actions API with cookie bootstrap); ASX instrument master live-confirmed (no ISIN/lot_size/tick_size in that feed); no ASX bulk corporate-actions endpoint found reachable -- ASX split/bonus/dividend relies on manual override table (shared/instruments/manual_overrides.yaml), a known documented gap not a silent one (ADR-010); TA-Lib's resolved wheel now imported (M04) means apply_schema-based CLIs share the same TimescaleDB schema file across M03/M05; adjustment engine covers SPLIT/BONUS only -- DIVIDEND/SYMBOL_CHANGE recorded but not price-adjusted (ADR-010); new env var: none |
| M06 | Pattern Recognition Engine | complete | 2026-07-01 | 363 tests total (361 passing + 2 skipped with Redis/TimescaleDB up: 318 unit + 43 integration; 318 passing + 2 skipped unit-only), 97% coverage on shared/+apps/ (engine/models/SR at 100%), ruff clean, mypy --strict clean (133 files), 6 M06 VERIFY integration tests pass against real TimescaleDB: ORB range + bullish/bearish breakout correct, S/R support/resistance at known bounce prices with touch counts, CDLENGULFING detected bullish at exact known bar index. Full M01-M06 validation pass run 2026-07-01: all 363 tests confirmed, ruff+mypy clean, all 6 modules spec-aligned. | CDL scanner uses `dir(talib)` for dynamic function discovery (future TA-Lib upgrades auto-included); ORB session_open lower-bound clamp required to exclude pre-session candles (found via failing test); S/R clusters before counting touches to avoid double-counting within a zone; `compute_multi_timeframe` accepts `Mapping` not `dict` for covariant typing; CDL_MIN_CANDLES=10 covers all TA-Lib CDL lookback periods; `__main__.py` supports `--timeframes 1m,5m,15m` for multi-TF cross-confirmation; no new env vars; post-commit ruff format fix: orb.py list comprehension reformatted to single line (commit 227f76c) |
| M07 | Backtesting Engine | complete | 2026-07-01 | 403 tests passing (52 skipped: integration tests requiring live DB), 81 new M07 unit tests all passing, ruff clean, mypy --strict clean (155 files). VERIFY: EMA crossover on synthetic RELIANCE/NSE 252-day series, avg_slippage_bps > 0 (log-normal model confirmed). | T+1s markout not computable from candle data (requires tick-level M16 fills); only T+1m and T+5m markout offsets implemented. `fit_from_fills()` is forward-looking — refits log-normal params once M16 provides real broker fill data. Integration VERIFY tests skip when TimescaleDB unavailable. plotly pinned to 5.24.1 (ADR-012). |
| M08 | Market Regime Classifier | complete | 2026-07-01 | 488 tests passing (58 skipped: 52 prior DB/Redis integration + 6 new M08 integration), 85 new M08 unit tests all passing, ruff clean, mypy --strict clean (97 files). VERIFY integration tests skip cleanly without live TimescaleDB. M01–M08 independent audit complete (commit 1c55be6, 2026-07-01): all findings resolved — see ADR-013/ADR-014. | HMM hidden states are mapped to regime labels post-training by RF majority-vote (state mapping stored with the model); publisher uses cast() to work around redis-py 5.x `xadd`/`xrevrange` stubs typing Awaitable|Any; `fit_from_fills()` equivalent deferred until M16 provides real tick data for training labels. Audit fixes: promote_classifier Any→BacktestMetrics; list_model_versions logs on exception; 5 CLI standalones catch db_connection_failed; get_ticker_open_time renamed (was get_ticker_group_open_time); README.md added for all modules. |
| M09 | Stock Universe Filter | complete | 2026-07-01 | 591 unit tests passing (58 skipped: integration tests requiring live TimescaleDB/Redis), ~107 new M09 unit tests all passing, ruff clean, mypy --strict clean (15 M09 source files). VERIFY CLI confirmed (`python -m shared.universe --help`). Core module coverage: filter.py 100%, models.py 100%, scoring.py 95%, repository.py 90%, compliance.py 75% (network-dependent _fetch_url untested; overall non-CLI coverage 86.5% ≥ 85% threshold). | SentScore wired to 0.0 until M10 delivers the sentiment feed — β_Sent weight (0.10) is reserved, not dropped. Compliance source fails-open if NSE API is unreachable (empty exclusion set, not a halt). Redis watchlist TTL = 8 hours — M11 should re-score at market open rather than relying on the cache. Strategy ID in watchlist is a pre-signal hint; M11 may override after full gate evaluation. structlog PrintLoggerFactory fix: removed `file=sys.stdout` to allow dynamic sys.stdout resolution (ADR-015); prevents ValueError on closed file when capsys is used in tests. |
| M10 | Sentiment & News Agent | complete | 2026-07-01 | 742 unit tests passing (58 skipped: integration tests requiring live TimescaleDB/Redis), 151 new M10 unit tests all passing, ruff clean, mypy --strict clean (10 M10 source files). VERIFY scenario validated: 20 headlines scored → second run all cache hits → cost = 0. Core module coverage: models.py 100%, agent.py 95%, cost_tracker.py 96%, cache.py 88%, feeds.py 87%, market_indicators.py 87%, scorer.py 86% (all ≥ 85%). | GPTCache integration uses embedding-only path (no FAISS, no LLM adapter) — see ADR-016. litellm moved to module-level import to enable @patch in tests; mypy skip added for litellm stubs to avoid AssertionError — see ADR-017. India VIX + FII/DII fail-open: return None on network failure, agent proceeds with headline-only aggregate. SentScore feed now live — M11 Gate 8 reads MarketSentiment.aggregate_score. *.md excluded from ruff E501 (Markdown line length enforcement was pre-existing, not M10-specific). |
| M11 | Signal Generation Agent | complete | 2026-07-02 | 865 unit tests passing (58 skipped: integration), 123 new M11 tests all pass, 94% M11 coverage, ruff clean, mypy --strict clean. VERIFY: LONG/SHORT signals generated at 0.93 confidence; HIGH_VOL_CHAOS blocked at Gate 1. | Gate 3 uses VOLUME_DELTA close-vs-open proxy until M16 provides tick-level aggressor data. Gate 8 divergence is RSI/MACD approximation — true peak-vs-peak divergence needs successive snapshots (M16/M18). LLM explain uses Groq 70B async post-publish; falls back silently to empty string. |
| M12 | Risk & Position Sizing Engine | complete | 2026-07-02 | 973 unit tests passing (58 skipped: integration), 108 new M12 tests all pass, 98% M12 coverage, ruff clean, mypy --strict clean. VERIFY: all 6 scenarios correct — normal approved, circuit breaker blocked, halted blocked, portfolio heat blocked, snapshot 0.5× size, correlation blocked. | Kelly is OFF by default; requires paper-trading validation. Circuit breaker detects -2% breach and blocks; Kill Switch execution (cancel + exit + Redis flag + Telegram) is M18's responsibility. Correlation guard skips when < 2 data points. |
| M13 | Compliance & Regulatory Engine | complete | 2026-07-02 | 163 unit tests, ruff clean, mypy --strict clean. VERIFY 20/20 scenarios correct — all non-compliant orders blocked, all compliant orders approved with correct strategy tags. | MPP conversion is execution-quality practice; confirm legal mandate status with broker compliance before citing as hard SEBI rule in audit docs (change-log item 5). CP 386 is not yet final (proposed commencement Apr 2027) — build to 3-tier standard regardless. `is_priority` on KillSwitchEvent set ONLY by KillSwitchManager — no signal/entry code may access it. |
| M14 | Order Execution Engine | complete | 2026-07-02 | 1202 unit tests passing (66 new M14 tests), ruff clean, mypy --strict clean. VERIFY 20/20 scenarios correct — all 5 strategies, partial fill, SL exit, kill-switch liquidation, duplicate idempotency (no double fill), retry, dead-letter, compliance rejection, kill-switch halted (non-priority blocked, priority bypasses), MPP conversion, audit log complete. | Kite/IBKR broker adapters are stubs until M15 provides auth. `is_priority=True` settable ONLY by `make_sl_exit_order()` and `make_kill_switch_liquidation_order()` — no other call site. Dead-letter queue uses Redis `dlq:orders` with in-memory fallback. `sl_quantity` always equals `filled_quantity`, never `requested_quantity`. |
| M15 | Authentication & Token Manager | complete | 2026-07-02 | 1271 unit tests passing (65 new M15 tests), ruff clean, mypy --strict clean. VERIFY 20/20 scenarios correct — TOTP code gen, paper token, Redis TTL, expiry eviction, in-memory fallback, secrets absent from logs, IBKR pool acquire/release/exhaustion, paper/live ports, heartbeat daemon, cache-hit get_token, invalidate re-login, mock HTTP flow, scheduler fire, audit repr. | kiteconnect SDK not installed in dev — exchange_token() falls back to request_token directly (safe for paper mode; real env will have SDK). ibapi not installed in dev — connect() returns False, real TWS connection tested in M16/M23. DailyRefreshScheduler uses threading.Timer, no APScheduler dependency. |
| M16 | Data Ingestion Agent | complete | 2026-07-02 | 1333 unit tests passing (62 new M16 tests), ruff clean, mypy --strict clean. VERIFY 20/20 scenarios correct — zero-price/negative/future tick rejected, sequence reversal rejected, reset allows re-sequence, single tick no candle, bar roll emits candle, OHLCV correct, volume accumulates, flush/reset, Redis+memory buffer, inject_tick triggers candle, get_latest_candle, 1000-tick latency 17.7ms (budget 200ms). | kiteconnect/ibapi not installed — KiteWS and IBKRStream adapters degrade gracefully to disconnected stubs. yfinance fallback dev-only. DEGRADED_EXIT_ONLY flag written to Redis key `system:status:degraded` on WS timeout. |
| M17 | Reconciliation Agent | complete | 2026-07-02 | 1398 unit tests passing (65 new M17 tests), ruff clean, mypy --strict clean. VERIFY 20/20 scenarios correct — position qty/price/missing/unexpected mismatches, order status/missing/unexpected mismatches, case-insensitive status comparison, BlockRegistry block/clear/exchange discrimination, MismatchPublisher no-Redis, ReconciliationResult properties, full agent cycle clean/mismatch/stale-clear, on_mismatch callback, start/stop timer lifecycle. | kiteconnect/ibapi broker adapters injected via BrokerStateProvider Protocol — paper stub used in tests. Redis fallback for BlockRegistry: in-memory set survives only current process. MismatchPublisher stream delivery is best-effort; block flag is the primary safety control. Broker API failure causes all internal positions to appear as POSITION_MISSING (intentionally conservative). |
| M18 | Agent Orchestrator (LangGraph) | complete | 2026-07-02 | 1509 unit tests passing (111 new M18 tests), ruff clean, mypy --strict clean. VERIFY 20/20 scenarios correct — state init, JSON roundtrip, WorkingMemory put/get/evict/delete, ShortTermMemory in-memory+Redis, ACT-R activation formula (+no-retrieval -inf), LongTermMemory store/retrieve/top-k ranking, ACTRMemory tier routing, clean cycle, circuit breaker at -2.5%, HIGH_VOL_CHAOS blocked (RULE 2), BULL_TREND allows signal, reconciliation block prevents signal, HITL interrupt at 75% capital, kill switch preempts pending HITL (RULE 8). | LangGraph stubs (0.0.30) incomplete — mypy `follow_imports = "skip"` added for langgraph/langchain (same pattern as litellm, ADR-017). `trigger_kill_switch()` updates local `self._state` immediately before LangGraph invoke — guarantees `is_halted()` returns True even when graph has no pending interrupt checkpoint. `enable_hitl=False` in paper/automated runs. ACT-R Long-Term Memory falls back to in-process dict when PostgreSQL unavailable. |
| M19 | Real-Time Monitor Agent | not started | | | |
| M20 | Alerting & Notification | not started | | | |
| M21 | Reporting Module | not started | | | |
| M22 | Dashboard & API | not started | | | |
| M23 | Docker & Cloud Deployment | not started | | | |
